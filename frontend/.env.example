# APIのベースURL（本番は実サーバのURLを指定。空なら同一オリジン/proxy経由）
VITE_API_BASE_URL=

# 開発サーバ(npm run dev)が /api・/uploads を転送する先。空なら http://localhost:8000
# 例) Docker のデモスタックに繋ぐ場合や、8000 が他のアプリで埋まっている場合:
# VITE_API_PROXY_TARGET=http://localhost:8001
VITE_API_PROXY_TARGET=
