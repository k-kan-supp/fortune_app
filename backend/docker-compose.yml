services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: fortune
      POSTGRES_PASSWORD: fortune
      POSTGRES_DB: fortune
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
